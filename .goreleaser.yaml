release:
  # Repo in which the release will be created.
  # Default is extracted from the origin remote URL or empty if its private hosted.
  github:
    owner: arnaduga
    name: dump1090-simulator

  draft: true
  replace_existing_draft: true

  prerelease: auto

  extra_files:
    - glob: ./steps/*
